
name = "syncpad"
main = "worker.ts"
compatibility_date = "2024-01-01"

# Define the Durable Object binding
[durable_objects]
bindings = [
  { name = "SYNCPAD_DO", class_name = "SyncPadDO" }
]

# Migration to create the class
[[migrations]]
tag = "v1"
new_classes = ["SyncPadDO"]

# Configuration for serving static assets (if using Workers Sites)
[site]
bucket = "./dist"
